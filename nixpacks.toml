# Nixpacks 빌드 설정
# Railway가 사용하는 빌드 설정 파일
# 프로젝트 루트에서 실행되지만 backend 디렉토리로 이동

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "cd backend && npm install"
]

[phases.build]
cmds = [
  "echo 'Build complete - backend directory'"
]

[start]
cmd = "cd backend && npm start"

